# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load# Environment variables declared in this file are automatically made available to Prisma.
# See the documentation for more detail: https://pris.ly/d/prisma-schema#accessing-environment-variables-from-the-schema

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The below URL is configured for the local Prisma Postgres database. It doesn't allow connecting from outside of this
# workspace. This database will sleep after 5 minutes of inactivity. When it does, Prisma will start it up for you
# automatically. In case of issues, try connecting or creating a new workspace.
# For local development, this URL can be safely committed to source control. It can be used by Prisma Studio, and your
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="postgresql://postgres:Aliciaelamordemivida@127.0.0.1:5432/Microcredenciales_ESPOCH?schema=public"
JWT_SECRET="super_secret_jwt_key_12345"
AES_SECRET_KEY="la_clave_secreta_aes_256_de_32_bytes!!"

